/***************************************************
制作人：黄士祥
制作日期：2019.10.13
制作内容：已知圆柱体的底面半径r和高h，
计算圆柱体底面周长和面积、圆柱体侧面积以及圆柱体体积。
****************************************************/
#include <stdio.h>
#include <stdlib.h>
#define PI 3.1415926
int main()
{
	int r,h;//r为圆柱半径，h为高
	float c,s,s2,v;//c为底面周长，s为底面面积，s2为侧面积，v为体积
	scanf("%d%d",&r,&h);
	c=2*PI*r;
	s=PI*r*r;
	s2=c*h;
	v=s*h;
	printf("%.2f %.2f %.2f %.2f",c,s,s2,v);
system("pause");
return 0;
}